# move files into directories according to date

run: 

    python app.py src1 src2 ... dest

tools:
  PyExifTool - exiftool wrapper, slow
  EXIF.py (exifread) - can't read some files
  Pillow (PIL) -  can't read some files, can't read mov files

  mdls
  exiftool

